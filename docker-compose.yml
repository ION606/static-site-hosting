services:
  app:
    build: .
    ports:
      - "5121:5121"
    networks:
      - static
    volumes:
      - instance:/app/instance
      - ./sites:/app/sites
    environment:
      - FLASK_ENV=production
    user: "1000:1000"  # run as non-root user

networks:
  static:

volumes:
  instance: